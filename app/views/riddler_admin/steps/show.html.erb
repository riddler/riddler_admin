<div class="row">
  <div class="col-sm">
    <% if notice %>
    <div class="alert alert-success">
      <span id="notice"><%= notice %></span>
    </div>
    <% end %>
  </div>
</div>

<div class="row" style="margin-bottom: 32px;">
  <div class="col-sm-6">
    <h1><%= @step.title %></h1>
  </div>
  <div class="col-sm-6">
    <%= link_to 'Edit Step', edit_step_path(@step), class: 'btn btn-light float-right' %>
    <% if ::RiddlerAdmin.configuration.remote_riddler? %>
      <%= link_to "Publish", new_publish_request_path(step_id: @step.id), class: "btn btn-light float-right"%>
    <% end %>
  </div>
</div>

<%= render partial: @step.to_partial_path("show") %>
